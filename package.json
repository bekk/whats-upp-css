{
  "name": "slides",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server",
    "watch": "concurrently --kill-others \"npm run serve\" \"livereload .\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "caniuse-component": "^0.1.1",
    "remark-bekk": "github:bekk/remark"
  },
  "devDependencies": {
    "concurrently": "^3.1.0",
    "http-server": "^0.9.0",
    "livereload": "^0.6.0"
  }
}
